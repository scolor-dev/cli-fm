#[derive(Clone, Copy, Debug)]
pub enum Action {
    Up,
    Down,
    Enter,
    Back,
    Quit,
    EnterCommandMode,
    EnterSearchMode,
    EnterPathMode,
    EnterNormalMode,
    EnterRenameMode,
    InputChar(char),
    InputBackspace,
    InputSubmit,
    TabComplete,
    Copy,
    Cut,
    Paste,
    Delete,
    None,
}
